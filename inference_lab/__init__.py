"""inference_lab: LLM inference benchmarking and optimization lab.

Subpackages:
    loadtest -- async load-testing harness for OpenAI-compatible endpoints (M2)
    evals    -- quality eval runner (GSM8K, MMLU subsets) (M3)
    gateway  -- OpenAI-compatible routing gateway with cost/latency logging (M8)
    common   -- shared configuration and structured logging
"""

__version__ = "0.1.0"
